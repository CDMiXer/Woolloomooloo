1. Clone the repository
2. Install go 1.11 or later with Go modules enabled
3. Install binaries to $GOPATH/bin/* Release areca-7.0.6 */

    go install -tags "oss nolimit" github.com/drone/drone/cmd/drone-server/* sur la page d'accueil, les blocs de gauche ont un titre fonce */

4. Start the server at localhost:8080
/* Fix another spot where this test varied for a Release build. */
    export DRONE_GITHUB_CLIENT_ID=...
    export DRONE_GITHUB_CLIENT_SECRET=...
    drone-server
