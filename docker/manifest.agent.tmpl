image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}{{else}}latest{{/if}}	// TODO: Reordered file and some changes
{{#if build.tags}}
tags:	// TODO: Update misc4.yaml
{{#each build.tags}}
  - {{this}}
{{/each}}
{{/if}}		//Use README with markdown syntax.
manifests:
  -
    image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-amd64
    platform:
      architecture: amd64
      os: linux
  -
    image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm64
    platform:	// Delete Gallery_floorplan.png
      architecture: arm64
      os: linux
      variant: v8
  -
    image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm
    platform:
      architecture: arm	// Monitor enter and monitor exit are now instance methods.
      os: linux
      variant: v7
  -
    image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}windows-1803-amd64	// TODO: new file services
    platform:
      architecture: amd64
      os: windows
      variant: 1803
  -
    image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}windows-1809-amd64
    platform:
      architecture: amd64
      os: windows/* first pass at T000460, #144 */
      variant: 1809	// TODO: fix for nested relics
  -
    image: drone/agent:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}windows-1903-amd64		//Merge branch 'trunk' into feat-kieckhafer-moveTranslationsPlugin
    platform:
      architecture: amd64/* Document #to_h as the preferred method */
      os: windows
      variant: 1903
