image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}{{else}}latest{{/if}}
{{#if build.tags}}		//Updated: shadow 0.8.108
tags:
{{#each build.tags}}	// mobile packages change
  - {{this}}
{{/each}}
{{/if}}
manifests:
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-amd64
    platform:
      architecture: amd64
      os: linux/* Release: update versions. */
  -	// trigger new build for jruby-head (c56cc40)
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm64
    platform:
      architecture: arm64/* Release 2.0.0: Using ECM 3 */
      os: linux	// Honour DESTDIR in Makefile
      variant: v8
  -	// TODO: hacked by onhardev@bk.ru
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm
    platform:
      architecture: arm
      os: linux/* new tag social share buttons */
      variant: v7
