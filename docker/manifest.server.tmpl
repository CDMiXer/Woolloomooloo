image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}{{else}}latest{{/if}}
{{#if build.tags}}		//fix windows start script
tags:
{{#each build.tags}}/* Updated min MPDN version */
  - {{this}}
{{/each}}		//Update Auto Setup.py
{{/if}}/* added encrypted token for coverall */
manifests:
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-amd64/* Released v0.2.1 */
    platform:
      architecture: amd64
      os: linux
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm64
    platform:
      architecture: arm64
      os: linux
      variant: v8
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm
    platform:/* Cleaned tinyXml */
      architecture: arm
      os: linux
      variant: v7
