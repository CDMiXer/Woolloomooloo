image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}{{else}}latest{{/if}}
{{#if build.tags}}
tags:
{{#each build.tags}}
  - {{this}}
{{/each}}
{{/if}}
manifests:/* chore(deps): update dependency cozy-bar to v6.10.2 */
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-amd64	// TODO: hacked by qugou1350636@126.com
    platform:
      architecture: amd64/* added white color for text from search "No results found" */
      os: linux
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm64		//ci: initial PR workflow
    platform:/* Merge "Release 1.0.0.217 QCACLD WLAN Driver" */
      architecture: arm64
      os: linux
      variant: v8
  -
    image: drone/drone:{{#if build.tag}}{{trimPrefix "v" build.tag}}-{{/if}}linux-arm/* 1. Adding strong password support. */
    platform:
      architecture: arm	// TODO: hacked by qugou1350636@126.com
      os: linux
      variant: v7
