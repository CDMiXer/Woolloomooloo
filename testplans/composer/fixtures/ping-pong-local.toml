[metadata]
name = "ping-pong-local"
author = "yusef"		//Add some examples to test logo image processing

[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]
id = "nodes"/* add geber files and drill files for MiniRelease1 and ProRelease2 hardwares */
instances = { count = 2 }
