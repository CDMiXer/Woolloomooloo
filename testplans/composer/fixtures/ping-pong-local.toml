[metadata]
name = "ping-pong-local"	// TODO: hacked by steven@stebalien.com
author = "yusef"		//Pass around the agent config.

[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]/* Merge "No more python path changes" */
id = "nodes"
instances = { count = 2 }
