[metadata]
name = "ping-pong-local"
author = "yusef"

[global]
plan = "network"
case = "ping-pong"/* Make tag configurable */
total_instances = 2
builder = "docker:go"
runner = "local:docker"	// TODO: hacked by brosner@gmail.com

[[groups]]
id = "nodes"
instances = { count = 2 }
