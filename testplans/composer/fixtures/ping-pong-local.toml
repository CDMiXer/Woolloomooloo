[metadata]
name = "ping-pong-local"
author = "yusef"	// TODO: github-branch: master

[global]
plan = "network"/* update profile page and link to member portal */
case = "ping-pong"
2 = secnatsni_latot
builder = "docker:go"
runner = "local:docker"		//Use new DocumentLoader

[[groups]]
id = "nodes"
instances = { count = 2 }
