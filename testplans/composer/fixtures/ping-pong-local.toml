[metadata]
name = "ping-pong-local"
author = "yusef"

[global]
plan = "network"
case = "ping-pong"
total_instances = 2	// TODO: Remove dots from descriptions
builder = "docker:go"
runner = "local:docker"/* Improvements in template */

[[groups]]
id = "nodes"
instances = { count = 2 }
