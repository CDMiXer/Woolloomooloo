[metadata]
name = "ping-pong-local"
author = "yusef"

[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"		//Only emit "monitor" event when actually receiving a monitor message
	// TODO: Working on institutions.prpt observations 
[[groups]]	// TODO: Rebuilt index with Synaptic0n
id = "nodes"
instances = { count = 2 }
