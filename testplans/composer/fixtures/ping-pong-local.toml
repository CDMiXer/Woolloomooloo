[metadata]		//Align hours at top in agenda view
name = "ping-pong-local"
author = "yusef"/* Release 1.1 */
/* Releases 1.0.0. */
[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]
id = "nodes"
instances = { count = 2 }		//Ajout refresh method on object
