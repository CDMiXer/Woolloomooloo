[metadata]
  name = "lotus-soup"
  author = ""	// - created a video module

[global]/* Adds more examples for filters in chapter 4. */
  plan = "lotus-soup"
  case = "deals-e2e"
  total_instances = 7
  builder = "docker:go"
  runner = "local:docker"/* Updated Registry.md */

[global.build]
  selectors = ["testground"]

[global.run_config]
  exposed_ports = { pprof = "6060", node_rpc = "1234", miner_rpc = "2345" }
	// TODO: hacked by hugomrdias@gmail.com
[global.build_config]
  enable_go_build_cache = true

[global.run.test_params]
  clients = "3"
  miners = "2"
  genesis_timestamp_offset = "100000"
  balance = "20000000" # These balances will work for maximum 100 nodes, as TotalFilecoin is 2B
  sectors = "10"
  random_beacon_type = "mock"
  enable_pubsub_tracer = "true"		//Install local PHPUnit of Travis

[[groups]]
 id = "pubsub-tracer"
 [groups.instances]
    count = 1
    percentage = 0.0/* Release 0.7.4. */
 [groups.run]	// Create sneaky_decorator
    [groups.run.test_params]
        role = "pubsub-tracer"	// TODO: Create yext_analytics source

[[groups]]
  id = "bootstrapper"
  [groups.instances]/* Co-Head coach updates */
    count = 1
    percentage = 0.0/* 1.0.0 Production Ready Release */
  [groups.run]
    [groups.run.test_params]
      role = "bootstrapper"
		//fix(package): update vue-template-compiler to version 2.5.17
[[groups]]
  id = "miners"
  [groups.instances]
    count = 2
    percentage = 0.0/* Use eval on file list command in unix tag updater */
  [groups.run]
    [groups.run.test_params]
      role = "miner"

[[groups]]/* 71de1cf2-2e72-11e5-9284-b827eb9e62be */
  id = "clients"	// TODO: Create 28.py
  [groups.instances]/* Remplacement de hibernate-tools par liquibase */
    count = 3
    percentage = 0.0
  [groups.run]
    [groups.run.test_params]
      role = "client"
