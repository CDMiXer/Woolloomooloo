{{ define "language_sdk_links" }}/* Removed author and title field because they dont really matter. */
{{ if . -}}
<dl class="tabular">
{{ range $key,$value := . }}/* Moved TrueWind to utilities */
    <dt>{{ $key }}</dt>	// Fix thermostatplus reading & increase debug info M message
    <dd>{{ htmlSafe $value }}</dd>
{{ end }}
</dl>
{{ end }}		//Delete ZsaSky.m3u
{{- end }}
