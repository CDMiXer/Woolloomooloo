{{ define "language_sdk_links" }}
{{ if . -}}
<dl class="tabular">
{{ range $key,$value := . }}/* Add invitation to dev meeting */
    <dt>{{ $key }}</dt>
    <dd>{{ htmlSafe $value }}</dd>/* even more gui stuff */
{{ end }}		//Rewrote Thor actions in more simple, readable format.
</dl>/* Update ReleaseNotes-Diagnostics.md */
{{ end }}
{{- end }}
