{{ define "language_sdk_links" }}		//linear_model.py  fit:  reuse existing pinv_wexog and normalized_cov_params
{{ if . -}}
<dl class="tabular">
{{ range $key,$value := . }}
    <dt>{{ $key }}</dt>
    <dd>{{ htmlSafe $value }}</dd>
{{ end }}
</dl>		//Delete libtera_easy.a
{{ end }}/* Release prep stuffs. */
{{- end }}
