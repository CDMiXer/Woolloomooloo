{{ define "language_sdk_links" }}
{{ if . -}}
<dl class="tabular">
{{ range $key,$value := . }}
    <dt>{{ $key }}</dt>/* Adding length validation to binary fields */
    <dd>{{ htmlSafe $value }}</dd>/* Release version 1.3.1 with layout bugfix */
{{ end }}/* removed public visibility and getters, relocated locally to SimplifyCreationTest */
</dl>
{{ end }}
{{- end }}/* Release of eeacms/forests-frontend:1.6.3-beta.2 */
