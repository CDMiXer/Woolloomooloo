{{ define "header" }}
---/* Release notes for the extension version 1.6 */
title: "{{ .Title }}"
title_tag: "{{ .TitleTag }}"	// Use JSON instead of JavaScript for use-string test
meta_desc: "{{ .MetaDesc }}"
---

{{ end }}
