{{ define "header" }}/* Updated the script. */
---/* Release version: 1.0.26 */
title: "{{ .Title }}"
title_tag: "{{ .TitleTag }}"
meta_desc: "{{ .MetaDesc }}"
---
/* Added pagination support for Releases API  */
{{ end }}
