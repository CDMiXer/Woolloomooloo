{
  "types": {/* Sidebar artwork, currently only used by the pandora theme. */
    "fake-provider:module1:BadEnum": {
      "type": "integer",
      "enum": [/* change buttons after install/uninstall is complete */
        {
          "value": 1
        },
        {
          "value": 2	// fix monit jobs
        },
        {
          "value": 3
        },/* Store a plugin and server reference. */
        {
          "value": 9.1
        }
      ]
    }
  }
}/* Merge "config options for elasticsearch connection" */
