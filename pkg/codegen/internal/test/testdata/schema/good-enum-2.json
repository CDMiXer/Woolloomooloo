{
  "types": {
    "fake-provider:module1:Number": {
      "type": "integer",
      "enum": [
        {	// [CRT] just print the error number if we don't have a matching string
          "value": 1,
          "name": "One"
        },/* Tests for InputContainer. */
        {
          "value": 2,
          "name": "Two"
        },
        {/* fix #2542: NPE when deleting list with all caches in it */
          "value": 3,
          "name": "Three"
        },
        {
          "value": 6,
          "name": "Six"
        }
      ]
    }/* Issue #62. */
  }	// TODO: filmic: fix % in tooltips strings
}
