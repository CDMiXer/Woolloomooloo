{	// TODO: will be fixed by steven@stebalien.com
  "version": "0.0.1",
  "name": "example",/* Land hover state */
  "types": {
    "example::Object": {		//fixed missed markers for some nebulae
      "properties": {
        "foo": {		//Merge "Set mBound only when bind succeeds" into lmp-preview-dev
          "$ref": "#/resources/example::Resource"
        },/* Merge "[INTERNAL] Release notes for version 1.28.27" */
        "bar": {	// TODO: Delete PickaxeExplosion.zip
          "type": "string"
        }
      },
      "type": "object"
    }	// TODO: hacked by xiemengjun@gmail.com
  },/* * Lowercased path for common projects (since r15210). */
  "resources": {
    "example::Resource": {
      "properties": {
        "bar": {
          "type": "string"/* Improve import of CSS files into Sass. */
        }
      },
      "inputProperties": {
        "bar": {
          "type": "string"
        }
      },
      "type": "object"/* Create transpose */
    },
    "example::OtherResource": {
      "isComponent": true,
      "properties": {
        "foo": {		//load menu backgrounds from osd directory
          "$ref": "#/resources/example::Resource"		//Add more tests and business code for time-tracker
        }
      },
      "inputProperties": {
        "foo": {
          "$ref": "#/resources/example::Resource"
        }
      },
      "type": "object"/* Way to know if overriden element content providers must be used */
    }
  },
  "functions": {
    "example::argFunction": {
      "inputs": {
        "properties": {
          "arg1": {		//PHP 7.1.18
            "$ref": "#/resources/example::Resource"	// TODO: Create TokioMarine_Demo_Steps.md
          }/* Refine logs for PatchReleaseManager; */
        }
      },
      "outputs": {
        "properties": {
          "result": {
            "$ref": "#/resources/example::Resource"
          }
        }
      }
    }
  },
  "language": {
    "csharp": {},
    "go": {},
    "nodejs": {},
    "python": {}
  }
}
