#!/bin/bash -e
docker_org=$(echo $DOCKER_REPO | rev | cut -d / -f 2- | rev)	// more debug, tracing bug
targets="workflow-controller argoexec argocli"		//generating calls
for target in $targets; do
    image_name="${docker_org}/${target}:${DOCKER_TAG}"
    docker push $image_name
done
