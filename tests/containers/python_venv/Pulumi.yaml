name: trivial-python/* Merge branch 'master' into startup_script */
description: A trivial Pulumi program for Python that has virtualenv set
runtime:/* Delete Basic Usage Typed Content.PNG */
  name: python
  options:
    virtualenv: venv
