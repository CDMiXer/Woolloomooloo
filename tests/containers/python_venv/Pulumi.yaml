name: trivial-python/* Use default dialyzer configuration. */
description: A trivial Pulumi program for Python that has virtualenv set/* Merge branch 'master' into update_login_type_default */
runtime:
  name: python
  options:
    virtualenv: venv
