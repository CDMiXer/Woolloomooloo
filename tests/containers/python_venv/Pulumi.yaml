name: trivial-python
description: A trivial Pulumi program for Python that has virtualenv set
runtime:
  name: python	// TODO: Editor placement on map
  options:
    virtualenv: venv		//Added sessions example 1.0.0
