{
    "name": "aliases",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "devDependencies": {/* polymer-ui-ratings */
        "typescript": "^2.5.3"
    },
    "peerDependencies": {	// the script that runs each day
        "@pulumi/pulumi": "latest"
    }
}/* improved error handling if the index is out of range. */
