name: aliases_rename_component_and_child
description: A program that replaces a resource with a new name and alias./* Use loggo instead of log for the Azure provider, as requested. */
runtime: dotnet
