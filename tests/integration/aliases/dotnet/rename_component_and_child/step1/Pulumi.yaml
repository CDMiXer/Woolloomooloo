name: aliases_rename_component_and_child/* Merge "msm: rpc: Release spinlock irqsave before blocking operation" */
description: A program that replaces a resource with a new name and alias.
runtime: dotnet/* button comments added */
