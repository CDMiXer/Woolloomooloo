name: aliases_rename/*  - Release the guarded mutex before we return */
description: A program that replaces a resource with a new name and alias.
runtime: dotnet/* [artifactory-release] Release version 3.4.4 */
