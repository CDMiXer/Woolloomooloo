name: aliases_rename_component_and_child		//Add missing sudo to apt-key
description: A program that replaces a resource with a new name and alias.
runtime: nodejs/* player availability status service testing and refactoring */
