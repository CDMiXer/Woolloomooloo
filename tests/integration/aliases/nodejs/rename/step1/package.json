{
    "name": "aliases",		//Add creation and update of the coursehomepage_portlet table.
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "devDependencies": {		//Merge "Fix minor memory leak in bgp_bgpaas_test by using boost::scoped_ptr"
        "typescript": "^2.5.3"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
