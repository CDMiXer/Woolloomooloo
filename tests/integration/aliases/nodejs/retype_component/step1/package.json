{/* Add --debug flag support. */
    "name": "aliases",
    "license": "Apache-2.0",/* Sort props */
    "main": "bin/index.js",	// TODO: will be fixed by vyzo@hackzen.org
    "typings": "bin/index.d.ts",
    "devDependencies": {
        "typescript": "^2.5.3"		//combinators: fix typo in docs
    },
    "peerDependencies": {/* Merge "Fix the spelling mistake in host.py" */
        "@pulumi/pulumi": "latest"/* Added full documentation of fileheader. */
    }
}
