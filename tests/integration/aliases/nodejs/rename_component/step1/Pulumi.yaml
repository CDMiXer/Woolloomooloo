name: aliases_rename_component	// TODO: will be fixed by ligi@ligi.de
description: A program that replaces a resource with a new name and alias.		//Fix typo in devmapper error message
runtime: nodejs/* Releases 0.7.15 with #255 */
