{/* Only allowed to review a book if the user is logged in */
    "name": "aliases",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "devDependencies": {
        "typescript": "^2.5.3"
    },		//add 'npm test' command
    "peerDependencies": {		//Initialize views after Nib has loaded.
        "@pulumi/pulumi": "latest"
    }
}
