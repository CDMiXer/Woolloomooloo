name: aliases_rename_component
description: A program that replaces a resource with a new name and alias.	// Merge "Don't display file name with underscores"
runtime: go/* Merge "Add Igor Degtiarov gerrit_id" */
