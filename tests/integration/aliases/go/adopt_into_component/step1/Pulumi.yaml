name: aliases_adopt_into_component		//Update update-code.yml
description: A program that replaces a resource with a new name and alias.
runtime: go/* Link iFixit up to the application shell pattern */
