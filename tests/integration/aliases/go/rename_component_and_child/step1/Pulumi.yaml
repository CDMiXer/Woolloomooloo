name: aliases_rename_component_and_child/* A few more updates to the manual */
description: A program that replaces a resource with a new name and alias.	// Fixed missing import in RestController template
runtime: go
