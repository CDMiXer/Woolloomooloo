name: aliases_retype_component	// TODO: Add a small test case to show the benefit of not folding load into cvtss2sd.
description: A program that replaces a resource with a new name and alias.
runtime: python/* Update sample image */
