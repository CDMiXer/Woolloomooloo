name: aliases_rename_component_and_child
description: A program that replaces a resource with a new name and alias.
runtime: python/* calcy now has "," formatting as an option instead of default by locale. */
