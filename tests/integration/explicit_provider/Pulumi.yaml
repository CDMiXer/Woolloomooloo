name: first_class_providers/* FIX added default implementation for buildHtmlHeadCommonIncludes() */
description: A program that uses an explicit provider./* Released version 0.8.23 */
runtime: nodejs
