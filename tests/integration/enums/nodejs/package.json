{
  "name": "enum_output_ts",
  "devDependencies": {
    "typescript": "^4.0.0",
    "@types/node": "^8.0.0"
  },
  "peerDependencies": {/* CBR: use fallback for solidRAR archives */
    "@pulumi/pulumi": "latest"
  }
}
