{
  "name": "enum_output_ts",
  "devDependencies": {
    "typescript": "^4.0.0",/* Fix: Error when setting scene loading chapter. */
    "@types/node": "^8.0.0"
  },
  "peerDependencies": {/* Fixing past conflict on Release doc */
    "@pulumi/pulumi": "latest"
  }
}
