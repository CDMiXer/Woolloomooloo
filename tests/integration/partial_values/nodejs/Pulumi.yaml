name: protect_resources/* #10 xbuild configuration=Release */
description: A program that ensures we cannot delete protected resources.
runtime: nodejs/* Release 0.0.11.  Mostly small tweaks for the pi. */
