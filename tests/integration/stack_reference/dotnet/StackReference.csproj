<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>		//fixed test cases with braces
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp3.1</TargetFramework>/* [Cleanup] Remove CConnman::Copy(Release)NodeVector, now unused */
  </PropertyGroup>	// TODO: Fixed UniGitData being created in constructors

  <PropertyGroup>	// Delete pandalocker.min.js
    <DefaultItemExcludes>$(DefaultItemExcludes);step*\**</DefaultItemExcludes>
  </PropertyGroup>

</Project>		//Added trailing semicolon to shim module definition
