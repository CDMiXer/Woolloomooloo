name: config_basic_py
description: A simple Python program that uses configuration./* Merge "Add --public to setup-endpoints." */
runtime: python	// TODO: Updating tests to their expected values for left join GIs
