name: project_main_parent
description: A program with an entry point in a parent folder (should fail)		//chore(Dockerfile): Add workflow dependencies
runtime: nodejs/* Delete ganjacoin-qt */
main: ./foo/../../bar
