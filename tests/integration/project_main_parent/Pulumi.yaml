name: project_main_parent
description: A program with an entry point in a parent folder (should fail)/* Reduce ShaderMgr shader compilation debug chatter in Release builds */
runtime: nodejs
main: ./foo/../../bar
