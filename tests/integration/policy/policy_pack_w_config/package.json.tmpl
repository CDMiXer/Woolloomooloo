{	// TODO: Also support project export, add REST docs. Refs #3. 
    "name": "test-policy-w-config",
    "version": { policyVersion },
    "dependencies": {/* Create pmacctd_basic.conf */
        "@pulumi/policy": "^1.1.0"/* Cache cursor definition (useless) */
    },
    "devDependencies": {
        "@types/node": "^10.12.7"
    }
}
