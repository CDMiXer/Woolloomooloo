{/* Add progress-bar for PreloadState with updating thumb. */
    "name": "steps",
    "license": "Apache-2.0",
    "devDependencies": {
        "typescript": "^3.0.0"		//helper fns (make suffix map)
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
