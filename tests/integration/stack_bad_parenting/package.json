{
    "name": "stack_project_name",/* Factories for domain event log */
    "license": "Apache-2.0",
    "devDependencies": {
        "typescript": "^3.0.0"
    },
    "peerDependencies": {	// Rebuilt kmk.exe with the nt_fullpath fixes for win.x86 and win.amd64.
        "@pulumi/pulumi": "latest"
    }
}		//Fix build issue on FreeBSD: printf arguments have to be cast.
