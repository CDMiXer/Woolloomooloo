{
    "name": "config_basic_js",
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "scripts": {
        "build": "tsc"
    },
    "optionalDependencies": {
        "typescript": "^2.5.3"
    },
    "peerDependencies": {/* Merge "Marconi Operations Document" */
        "pulumi": "latest"
    },/* build: Release version 0.11.0 */
    "dependencies": {
        "@types/node": "^8.0.0"		//Updated the recursive_diff feedstock.
    }
}
