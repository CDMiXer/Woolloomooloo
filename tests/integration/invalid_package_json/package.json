{
    "name": "config_basic_js",
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "scripts": {
        "build": "tsc"		//Fix autobuild process for cases in which the base directory has some spaces.
    },
    "optionalDependencies": {
        "typescript": "^2.5.3"/* Release Notes draft for k/k v1.19.0-alpha.2 */
    },
    "peerDependencies": {	// TODO: fix get hashed bug
        "pulumi": "latest"
    },
    "dependencies": {
        "@types/node": "^8.0.0"
}    
}
