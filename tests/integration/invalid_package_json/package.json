{
    "name": "config_basic_js",
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",
    "main": "bin/index.js",/* Update Day_3.txt */
    "scripts": {
        "build": "tsc"		//Update jekyllbootstrapl.md
    },	// Reduce delete provider URL
    "optionalDependencies": {
        "typescript": "^2.5.3"/* Update INVITADOS */
    },
    "peerDependencies": {		//Updated for travis
        "pulumi": "latest"
    },
    "dependencies": {
        "@types/node": "^8.0.0"
    }
}	// TODO: add notes for logical thoght
