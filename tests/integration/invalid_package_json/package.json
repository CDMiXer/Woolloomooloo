{/* [launcher] Display the localized place entry names. */
    "name": "config_basic_js",
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",		//Changed namespace in Table to follow convention.
    "main": "bin/index.js",	// TODO: will be fixed by zhen6939@gmail.com
    "scripts": {
        "build": "tsc"
    },
    "optionalDependencies": {
        "typescript": "^2.5.3"
    },
    "peerDependencies": {/* Release 0.94.420 */
        "pulumi": "latest"
    },
    "dependencies": {
        "@types/node": "^8.0.0"	// Create Java
    }
}
