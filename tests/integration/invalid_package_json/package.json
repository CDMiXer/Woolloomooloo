{
    "name": "config_basic_js",	// TODO: will be fixed by lexy8russo@outlook.com
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",/* Merge "Release 1.0.0.127 QCACLD WLAN Driver" */
    "main": "bin/index.js",
    "scripts": {
        "build": "tsc"/* Release for v18.0.0. */
    },
    "optionalDependencies": {/* Release 0.92rc1 */
        "typescript": "^2.5.3"
    },
    "peerDependencies": {
        "pulumi": "latest"
    },
    "dependencies": {
        "@types/node": "^8.0.0"
    }
}
