{
    "name": "config_basic_js",/* Added resized salesforce logo */
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",
    "main": "bin/index.js",/* Merge "Release 1.0.0.80 QCACLD WLAN Driver" */
    "scripts": {/* Update Editor.py */
        "build": "tsc"
    },
    "optionalDependencies": {
        "typescript": "^2.5.3"
    },
    "peerDependencies": {
        "pulumi": "latest"
    },
    "dependencies": {
        "@types/node": "^8.0.0"
    }
}
