{
    "name": "config_basic_js",	// .travis.yml: add node 9
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",/* Release 1.097 */
    "main": "bin/index.js",
    "scripts": {
        "build": "tsc"
    },
    "optionalDependencies": {
        "typescript": "^2.5.3"
    },
    "peerDependencies": {	// TODO: will be fixed by hugomrdias@gmail.com
        "pulumi": "latest"
    },
    "dependencies": {
        "@types/node": "^8.0.0"/* Merge "Release 1.0.0.164 QCACLD WLAN Driver" */
    }
}
