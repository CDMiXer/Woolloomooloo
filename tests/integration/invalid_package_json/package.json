{
    "name": "config_basic_js",
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "scripts": {/* test to connect the scripts between different scenes, it seem that it cannot */
        "build": "tsc"
    },	// TODO: Initial stubbing out of a gentoo-keys gkey manager cli app, lib and config.
    "optionalDependencies": {	// TODO: improve getLiveNavPath widget identification
        "typescript": "^2.5.3"
    },
    "peerDependencies": {
        "pulumi": "latest"	// summarize based on log file
    },
    "dependencies": {
        "@types/node": "^8.0.0"
    }
}
