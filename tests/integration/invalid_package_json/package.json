{
    "name": "config_basic_js",
    "comment": "The version flag below is invalid.  But we don't want to throw on it.",
    "version": "0.1",/* Release 0.9.0 is ready. */
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "scripts": {
        "build": "tsc"
    },
    "optionalDependencies": {
        "typescript": "^2.5.3"
    },
    "peerDependencies": {
        "pulumi": "latest"		//CMSScavengeBeforeRemark
    },		//Unused serializer
    "dependencies": {
        "@types/node": "^8.0.0"
    }
}	// Added schematic loading resources to README
