name: invalid_package_json/* Merge "[Release] Webkit2-efl-123997_0.11.77" into tizen_2.2 */
runtime: nodejs	// update valid timestamp in current table too!
description: Validates codepath computation even with an invalid version in the package.json file.
