name: invalid_package_json/* Added optional keypress callback optional utf8 parameter on Windows. */
runtime: nodejs
description: Validates codepath computation even with an invalid version in the package.json file.
