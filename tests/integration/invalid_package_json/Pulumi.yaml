name: invalid_package_json	// TODO: will be fixed by mail@bitpshr.net
runtime: nodejs
description: Validates codepath computation even with an invalid version in the package.json file./* Merge "Wlan: Release 3.8.20.18" */
