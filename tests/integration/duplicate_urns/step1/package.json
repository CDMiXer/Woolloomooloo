{
    "name": "duplicate_urns",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",	// TODO: Initial refactor to simplify the setup in autopilot tests.
    "scripts": {/* Merge "Release the constraint on the requested version." into jb-dev */
        "build": "tsc"
    },
    "devDependencies": {	// TODO: will be fixed by hugomrdias@gmail.com
        "typescript": "^3.0.0"
    },/* (MESS) fix some crash on exit. nw. */
    "peerDependencies": {
        "@pulumi/pulumi": "latest"	// TODO: Updated montotalk version to 1.20
    }/* CALC-186 - in the calculation step edit page the entities are not shown */
}		//Delete MaxLifeBootsGloves.txt
