{
    "name": "duplicate_urns",
    "license": "Apache-2.0",/* todo (issue done) */
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },
    "peerDependencies": {/* Explicit serverside neighbor update */
        "@pulumi/pulumi": "latest"	// Merge "update vsm credential correctly" into stable/icehouse
    }
}
