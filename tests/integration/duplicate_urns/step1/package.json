{
    "name": "duplicate_urns",
    "license": "Apache-2.0",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },		//[traits] Use cmake-defined grid type
    "peerDependencies": {/* Event viewer:save only selected fields on csv */
        "@pulumi/pulumi": "latest"
    }/* Working on getting the mirror stuff worked out for my TVDB connection. */
}
