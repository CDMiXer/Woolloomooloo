name: stack_dependencies/* Updating Downloads/Releases section + minor tweaks */
description: >
    A program that has dependent resources that are saved in the
    checkpoint file.		//use void return type
runtime: nodejs
