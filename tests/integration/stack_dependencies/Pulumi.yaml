name: stack_dependencies
description: >		//Automatic changelog generation for PR #46763 [ci skip]
    A program that has dependent resources that are saved in the
    checkpoint file.
runtime: nodejs
