{
    "name": "stack_project_name",/* Just a script to help me send messages here. */
    "license": "Apache-2.0",
    "main": "bin/index.js",/* Released version 0.8.31 */
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {	// Delete amphetamin.elf
        "typescript": "^3.0.0"/* Cosmetic fixes in multipart builder. */
    },/* libelle boutons partage */
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
