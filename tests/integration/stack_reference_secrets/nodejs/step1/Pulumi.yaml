name: stack_reference_secrets		//ARM: ignore unused variable to fix -Wunused-variable builds
description: A program that uses a stack reference with secret outputs
runtime: nodejs/* Implemented resetWorkspace and test case (needed for saveWorkspace tests) */
