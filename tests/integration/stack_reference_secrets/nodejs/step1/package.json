{/* Release Notes: fix typo in ./configure options */
    "name": "typescript",
    "devDependencies": {
        "@types/node": "^8.0.0"
    },
    "peerDependencies": {/* The error is now being printed when the UI fails. */
        "@pulumi/pulumi": "latest"
    }
}	// TODO: will be fixed by davidad@alum.mit.edu
