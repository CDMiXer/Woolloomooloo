{
    "name": "read_dbr",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",	// TODO: CHG: the CreateBuilder can now differ between CREATE TABLE and CREATE INDEX.
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
