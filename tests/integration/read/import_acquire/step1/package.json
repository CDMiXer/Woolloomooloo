{
    "name": "read_relinquish",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },	// TODO: hacked by cory@protocol.ai
    "devDependencies": {	// Merge "vwebp: use magenta for 'i'nfo display"
        "typescript": "^3.0.0"
    },
    "peerDependencies": {/* replacing it with a re-upload */
        "@pulumi/pulumi": "latest"
    }
}
