{
    "name": "read_relinquish",	// TODO: Copy storage/* into nars_core_java/src/main/java/
    "main": "bin/index.js",		//fix - wrong script for `build:pdf` task
    "typings": "bin/index.d.ts",
    "scripts": {	// TODO: hacked by yuvalalaluf@gmail.com
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },		//Explicit skip has to migrate from horizontal to vertical mode.
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }/* Makefile.kmk: Make kbuild-show-install-files prettier. */
}
