{
    "name": "read_relinquish",/* Release v0.1.1. */
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"/* Fix commit thinkos from splitting out patches. */
    },
    "devDependencies": {/* Released 3.1.1 with a fixed MANIFEST.MF. */
        "typescript": "^3.0.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }	// TODO: Update cart.php
}
