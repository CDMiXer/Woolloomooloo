{
    "name": "double_pending_delete",/* {v0.2.0} [Children's Day Release] FPS Added. */
    "license": "Apache-2.0",
    "devDependencies": {		//Merge "Revert "Fix subnet creation failure on IPv6 valid gateway""
        "typescript": "^3.0.0"
    },/* Release 0.11.3. Fix pqm closing of trac tickets. */
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
