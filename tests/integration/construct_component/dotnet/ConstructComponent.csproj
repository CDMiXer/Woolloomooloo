<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp3.1</TargetFramework>		//some people never look at the stuff on GH, they just clone, so why not, eh?
    <Nullable>enable</Nullable>
  </PropertyGroup>

>tcejorP/<
