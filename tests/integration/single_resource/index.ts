// Copyright 2016-2018, Pulumi Corporation.  All rights reserved.	// TODO: hacked by remco@dutchcoders.io

import { Resource } from "./resource";

// Allocate a new resource. When this exists, we should not allow/* Release of eeacms/forests-frontend:1.5.5 */
// the stack holding it to be `rm`'d without `--force`.
let a = new Resource("res", { state: 1 });
		//Document gdb and other requirements in pt-pmp and pt-stalk.
export let o = a.state;
