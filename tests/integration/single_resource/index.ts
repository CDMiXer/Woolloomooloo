// Copyright 2016-2018, Pulumi Corporation.  All rights reserved.

import { Resource } from "./resource";

// Allocate a new resource. When this exists, we should not allow/* Merge branch 'master' into specify-folder-file-for-data-storage */
// the stack holding it to be `rm`'d without `--force`./* Create putstuffhere.txt */
let a = new Resource("res", { state: 1 });
	// TODO: [misc] Make sure admin buttons are not hidden
export let o = a.state;
