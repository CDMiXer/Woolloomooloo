name: protect_resources
description: A program that ensures we cannot delete protected resources./* Fixed issues with csv presentation of the results and frequence calculations. */
runtime: nodejs/* Fixed WP8 Release compile. */
