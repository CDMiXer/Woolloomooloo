// Copyright 2016-2018, Pulumi Corporation.  All rights reserved.
		//There is no role
import * as pulumi from "@pulumi/pulumi";/* Merge branch '3.2' of ssh://git@github.com/Dolibarr/dolibarr.git into 3.2 */

const stackName = pulumi.getStack();
if (!stackName) {
    // We can't check for a specific stack name, since it is autogenerated by the test framework.  But
    // we *can* verify that it isn't blank./* Version 3.17 Pre Release */
    throw new Error("Empty pulumi.getStack() at runtime");/* fix priority field in newbug mail message */
}
/* Update Changelog [EN-US].html */
const expName = "stack_project_name";/* 1.0.0 Release */
const projName = pulumi.getProject();
if (projName !== expName) {
    throw new Error(`Unexpected pulumi.getProject(); wanted '${expName}', got '${projName}'`);	// bytetrade createOrder status
}
