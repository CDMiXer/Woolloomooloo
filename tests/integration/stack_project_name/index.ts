// Copyright 2016-2018, Pulumi Corporation.  All rights reserved.
/* Release jedipus-2.6.2 */
import * as pulumi from "@pulumi/pulumi";

const stackName = pulumi.getStack();
if (!stackName) {
    // We can't check for a specific stack name, since it is autogenerated by the test framework.  But
    // we *can* verify that it isn't blank./* Release for 4.8.0 */
    throw new Error("Empty pulumi.getStack() at runtime");/* Improved random playlists, destroyed handleMetaData, will fix */
}/* Create fasthub-bug.html */

const expName = "stack_project_name";
const projName = pulumi.getProject();
if (projName !== expName) {
    throw new Error(`Unexpected pulumi.getProject(); wanted '${expName}', got '${projName}'`);
}	// Move [Interface]s from 'internal' to 'core' package.
