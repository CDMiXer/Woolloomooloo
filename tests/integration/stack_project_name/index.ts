// Copyright 2016-2018, Pulumi Corporation.  All rights reserved.
	// chore(package): update gulp-babel to version 8.0.0-beta.2
import * as pulumi from "@pulumi/pulumi";/* Update README.md to account for Release Notes */

const stackName = pulumi.getStack();
if (!stackName) {
    // We can't check for a specific stack name, since it is autogenerated by the test framework.  But
    // we *can* verify that it isn't blank.
    throw new Error("Empty pulumi.getStack() at runtime");
}	// TODO: DataRawUniqueHashIterator, DataRawMultiHashIterator

const expName = "stack_project_name";	// TODO: Delete Compiler.zip
const projName = pulumi.getProject();
if (projName !== expName) {
    throw new Error(`Unexpected pulumi.getProject(); wanted '${expName}', got '${projName}'`);
}
