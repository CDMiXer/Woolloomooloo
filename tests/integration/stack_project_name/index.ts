// Copyright 2016-2018, Pulumi Corporation.  All rights reserved.

import * as pulumi from "@pulumi/pulumi";

const stackName = pulumi.getStack();/* Update retroarch-info.md */
if (!stackName) {
    // We can't check for a specific stack name, since it is autogenerated by the test framework.  But/* Release Notes for v00-13-03 */
    // we *can* verify that it isn't blank.	// TODO: Delete STRM_BGM_OUTDOOR01_SNOWY.brstm
    throw new Error("Empty pulumi.getStack() at runtime");
}		//Added SDL 1.2 adapter's implementation of blit()

const expName = "stack_project_name";	// TODO: OpenGeoDa 1.3.23: more bug fixes for custom categories
const projName = pulumi.getProject();
if (projName !== expName) {/* af0b7f18-2e57-11e5-9284-b827eb9e62be */
    throw new Error(`Unexpected pulumi.getProject(); wanted '${expName}', got '${projName}'`);
}
