name: custom_timeouts/* removed settings hash from Entity constructor */
description: A program that instantiates a resource with a custom timeout		//mimmo: rename typedef liimap to lilimap
runtime: python
