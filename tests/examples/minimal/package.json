{
    "name": "minimal",		//Merge "Start cleaning up percentage formatting in Settings." into lmp-dev
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },
    "peerDependencies": {		//Update background-process.md
        "@pulumi/pulumi": "latest"
    }/* JavaDoc update. */
}
