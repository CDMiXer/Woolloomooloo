{
    "name": "minimal",	// limit number of request insertions per minute to avoid overloading the DB
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"/* Release 1,0.1 */
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
