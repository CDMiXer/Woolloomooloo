{
    "name": "minimal",/* Merge "Release 1.0.0.207 QCACLD WLAN Driver" */
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"	// Merge "Removing code related to old page-actions menu markup"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
