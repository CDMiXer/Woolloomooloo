{
    "name": "minimal",
    "license": "Apache-2.0",/* use api v2 to display mobile platforms */
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {		//^ Updated the default character set of tables to utf8mb4
        "typescript": "^3.0.0"
    },
    "dependencies": {
        "@pulumi/pulumi": "=0.10.0"	// Update to valid json format
    }
}
