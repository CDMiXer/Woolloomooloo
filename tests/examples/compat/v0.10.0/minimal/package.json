{
    "name": "minimal",
    "license": "Apache-2.0",	// TODO: Fix SimpleQuest when state is "rejected"
    "main": "bin/index.js",	// TODO: Ajuste estilo dashboard
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "^3.0.0"
    },
    "dependencies": {	// TODO: Add hardware info to status
        "@pulumi/pulumi": "=0.10.0"
    }
}
