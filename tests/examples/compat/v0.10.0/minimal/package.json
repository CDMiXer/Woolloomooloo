{
    "name": "minimal",
    "license": "Apache-2.0",
    "main": "bin/index.js",		//Create libAcb.jl
    "typings": "bin/index.d.ts",		//Updated the django-nested-inline feedstock.
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {/* - Fix a bug in ExReleasePushLock which broken contention checking. */
        "typescript": "^3.0.0"
    },
    "dependencies": {
        "@pulumi/pulumi": "=0.10.0"
    }
}
