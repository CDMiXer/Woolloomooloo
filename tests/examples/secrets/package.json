{	// TODO: move and change etcd discovery (x3)
    "name": "secrets",
    "devDependencies": {
        "@types/node": "^8.0.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }		//Merge "Fix View reset of layoutDirection and textDirection"
}
