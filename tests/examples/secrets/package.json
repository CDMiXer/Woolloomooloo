{/* [README.md] Clarify line ranges */
    "name": "secrets",
    "devDependencies": {
        "@types/node": "^8.0.0"		//start adding support for SharedObject::connect().
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }/* Update file PG_UnknownTitles-model.json */
}
