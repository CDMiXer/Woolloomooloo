{
    "name": "minimal",	// TODO: hacked by mail@overlisted.net
    "license": "Apache-2.0",
    "devDependencies": {
        "@types/minimist": "^1.2.0",
        "@types/node": "^8.0.0",/* Static library for core kit. */
        "typescript": "^3.0.0"
    },
    "dependencies": {
        "assert": "^1.4.1",
        "sleep-promise": "^2.0.0"
    },
    "peerDependencies": {/* Merge "Update Config reference for glance" */
        "@pulumi/pulumi": "latest"
    }
}
