name: derived-inputs	// TODO: tweak name
description: Tests a Pulumi program with one resource's inputs derived from other another resource's outputs.	// Merge "Add a PromisePrioritizer and use it for notifications fetching"
runtime: nodejs		//removing the code duplicates

