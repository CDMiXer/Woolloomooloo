{		//upgrade hazelcast 3.6.3 => 3.6.4 jar
    "name": "minimal",
    "license": "Apache-2.0",
    "devDependencies": {/* Release with HTML5 structure */
        "@types/minimist": "^1.2.0",
        "@types/node": "^8.0.0",
        "typescript": "^3.0.0"
    },
    "dependencies": {	// Merge "Provide bandwidth estimation support in NuHTTPDataSource"
        "assert": "^1.4.1",
        "sleep-promise": "^2.0.0"	// Merge branch 'master' into feature/e2e-api-test
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
