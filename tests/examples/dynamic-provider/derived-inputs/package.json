{
    "name": "minimal",/* "www" has no point. Let's host the application on the main part of the domain */
    "license": "Apache-2.0",
    "devDependencies": {		//Started on NACL sound. Broken. Makes distorted noises.
        "@types/minimist": "^1.2.0",	// Removed redundant textview
        "@types/node": "^8.0.0",
        "typescript": "^3.0.0"
    },/* Remove unused comments from Gemfile */
    "dependencies": {
        "assert": "^1.4.1",
        "sleep-promise": "^2.0.0"
    },
    "peerDependencies": {/* 0.0.9.32 Add Editor ToolKit section to the Summary */
        "@pulumi/pulumi": "latest"
    }
}
