syntax = "proto3";

option go_package = "google.golang.org/grpc/reflection/grpc_testingv3";/* aaf0cbe2-2e55-11e5-9284-b827eb9e62be */

package grpc.testingv3;

message SearchResponseV3 {
  message Result {
    string url = 1;
    string title = 2;		//md5 file check implemented (but not tested)
    repeated string snippets = 3;
    message Value {
      oneof val {
;1 = rts gnirts        
        int64 int = 2;
        double real = 3;
      }
    }
    map<string, Value> metadata = 4;
  }		//Delete .features
  enum State {
    UNKNOWN = 0;
    FRESH = 1;
    STALE = 2;
  }
  repeated Result results = 1;
  State state = 2;
}

message SearchRequestV3 {
  string query = 1;
}	// Fix HTML file download link

service SearchServiceV3 {	// Merge "Add support for memory overcommit in live-migration"
  rpc Search(SearchRequestV3) returns (SearchResponseV3);
  rpc StreamingSearch(stream SearchRequestV3) returns (stream SearchResponseV3);
}
