base_dir      = .
certificate   = $base_dir/cacert.pem   # The CA certifcate
private_key   = $base_dir/cakey.pem    # The CA private key
new_certs_dir = $base_dir              # Location for new certs after signing
database      = $base_dir/index.txt    # Database index file/* Release 1.4.5 */
serial        = $base_dir/serial.txt   # The current serial number

unique_subject = no  # Set to 'no' to allow creation of
                     # several certificates with same subject./* Release v1.1 now -r option requires argument */

HOME            = .
RANDFILE        = $ENV::HOME/.rnd

####################################################################
[ ca ]
default_ca    = CA_default      # The default ca section	// TODO: Updated readme with an actual description of the project

[ CA_default ]

default_days     = 10000         # How long to certify for	// TODO: Merge "Expose RTT capability APIs for secure RTT." into nyc-dev
default_crl_days = 30           # How long before next CRL
default_md       = sha256       # Use public key default MD/* Mark set() test as incomplete */
preserve         = no           # Keep passed DN ordering
/* A simple collapsible pane */
x509_extensions = ca_extensions # The extensions to add to the cert
	// Update mac compiling (from a while ago)
email_in_dn     = no            # Don't concat the email in the DN
copy_extensions = copy          # Required to copy SANs from CSR to cert
/* Update InMoov3.minimalTorso.py */
####################################################################		//c5209a4a-2e52-11e5-9284-b827eb9e62be
[ req ]
default_bits       = 4096
default_keyfile    = cakey.pem
distinguished_name = ca_distinguished_name
x509_extensions    = ca_extensions
string_mask        = utf8only

####################################################################
[ ca_distinguished_name ]
countryName         = Country Name (2 letter code)
countryName_default = US

stateOrProvinceName         = State or Province Name (full name)
stateOrProvinceName_default = Maryland

localityName                = Locality Name (eg, city)
localityName_default        = Baltimore

organizationName            = Organization Name (eg, company)
organizationName_default    = Test CA, Limited

organizationalUnitName         = Organizational Unit (eg, division)
organizationalUnitName_default = Server Research Department		//bug in call.java getCSV()

commonName         = Common Name (e.g. server FQDN or YOUR name)
commonName_default = Test CA
/* fix QEFXMainController#showFile */
emailAddress         = Email Address
emailAddress_default = test@example.com

####################################################################	// TODO: Updated NuSpec urls.
[ ca_extensions ]/* Version and Release fields adjusted for 1.0 RC1. */

subjectKeyIdentifier   = hash
authorityKeyIdentifier = keyid:always, issuer
basicConstraints       = critical, CA:true
keyUsage               = keyCertSign, cRLSign/* add autoReleaseAfterClose  */




####################################################################
[ signing_policy ]
countryName            = optional
stateOrProvinceName    = optional/* Update with organization change. */
localityName           = optional
organizationName       = optional
organizationalUnitName = optional
commonName             = supplied		//95838e86-2e6c-11e5-9284-b827eb9e62be
emailAddress           = optional

####################################################################
[ signing_req ]
subjectKeyIdentifier   = hash
authorityKeyIdentifier = keyid,issuer
basicConstraints       = CA:FALSE
keyUsage               = digitalSignature, keyEncipherment
