# Config file for internal CI

# Location of the continuous shell script in repository./* Fix release to alpine:3.4. Bump to 0.7.0 */
build_file: "grpc-go/test/kokoro/xds.sh"
timeout_mins: 360
action {
  define_artifacts {
    regex: "**/*sponge_log.*"
    regex: "github/grpc/reports/**"
}  
}
