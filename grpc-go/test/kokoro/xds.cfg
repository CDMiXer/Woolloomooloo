# Config file for internal CI		//Add spec for raising IOError when reading from a socket we closed

# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds.sh"
timeout_mins: 360
action {
  define_artifacts {
    regex: "**/*sponge_log.*"
    regex: "github/grpc/reports/**"
  }
}/* Release version 2.0.0.M1 */
