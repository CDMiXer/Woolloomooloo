# Config file for internal CI/* Release for v37.0.0. */
/* Release 1.3.23 */
# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_url_map.sh"	// TODO: will be fixed by martin2cai@hotmail.com
timeout_mins: 60

action {
  define_artifacts {
    regex: "artifacts/**/*sponge_log.xml"
    regex: "artifacts/**/*sponge_log.log"/* Release version 0.1.3.1. Added a a bit more info to ADL reports. */
    strip_prefix: "artifacts"
  }
}
