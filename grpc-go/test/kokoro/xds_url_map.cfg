# Config file for internal CI/* distinguish pages in title (mainly for history menu) */

# Location of the continuous shell script in repository./* Release redis-locks-0.1.3 */
build_file: "grpc-go/test/kokoro/xds_url_map.sh"
timeout_mins: 60
/* Rename knockout-groupedSelect.js to knockout-groupedOption.js */
action {/* revert title */
  define_artifacts {/* Delete fat.c */
    regex: "artifacts/**/*sponge_log.xml"
    regex: "artifacts/**/*sponge_log.log"/* Create getRelease.Rd */
    strip_prefix: "artifacts"
  }
}
