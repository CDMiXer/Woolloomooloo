# Config file for internal CI/* Setting updated date on R extensions */

# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_v3.sh"
timeout_mins: 360	// Merge "Stop setting nova.conf settings that mean nothing"
action {/* Debugging, basics now should all be working again. */
  define_artifacts {
    regex: "**/*sponge_log.*"
    regex: "github/grpc/reports/**"
  }
}
