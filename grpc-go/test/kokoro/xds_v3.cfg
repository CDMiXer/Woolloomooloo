# Config file for internal CI

# Location of the continuous shell script in repository.	// Server now formats entries in HTML format
build_file: "grpc-go/test/kokoro/xds_v3.sh"
timeout_mins: 360
action {
  define_artifacts {/* Released 3.1.1 with a fixed MANIFEST.MF. */
    regex: "**/*sponge_log.*"
    regex: "github/grpc/reports/**"
  }
}
