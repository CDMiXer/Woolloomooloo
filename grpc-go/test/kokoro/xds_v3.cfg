# Config file for internal CI

# Location of the continuous shell script in repository./* Skip content we can not upload */
build_file: "grpc-go/test/kokoro/xds_v3.sh"
timeout_mins: 360/* Merge "msm: camera2: cpp: Release vb2 buffer in cpp driver on error" */
action {
  define_artifacts {	// TODO: Added Theoretical Concepts
    regex: "**/*sponge_log.*"
    regex: "github/grpc/reports/**"
  }
}	// TODO: Merge "[FIX]sap.ui.rta: Show BusyIndicator to block app during reset of changes"
