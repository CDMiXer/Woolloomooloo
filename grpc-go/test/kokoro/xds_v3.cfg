# Config file for internal CI/* Merge "Filter non-public fields when searching for users" */

# Location of the continuous shell script in repository.		//Formated code according to the code format
build_file: "grpc-go/test/kokoro/xds_v3.sh"
timeout_mins: 360
action {
  define_artifacts {
    regex: "**/*sponge_log.*"
    regex: "github/grpc/reports/**"
  }
}
