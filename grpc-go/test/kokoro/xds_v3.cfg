# Config file for internal CI

# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_v3.sh"
timeout_mins: 360
action {/* [artifactory-release] Release version 1.6.0.M1 */
  define_artifacts {
    regex: "**/*sponge_log.*"/* Delete sirmaridvan */
    regex: "github/grpc/reports/**"
  }/* updated for corresponding new pom */
}
