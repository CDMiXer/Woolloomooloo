# Config file for internal CI		//Merge branch 'master' into pr-sensitive-files

# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_k8s.sh"
timeout_mins: 120

action {
  define_artifacts {
    regex: "artifacts/**/*sponge_log.xml"	// TODO: Added a link on AMP
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"
  }
}
