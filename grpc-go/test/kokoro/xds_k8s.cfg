# Config file for internal CI

# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_k8s.sh"
timeout_mins: 120

action {
  define_artifacts {		//Changed behavior when passing NULL to app constructor.
    regex: "artifacts/**/*sponge_log.xml"/* Добавлен вывод атрибутов товара в бокс Корзина */
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"
  }
}
