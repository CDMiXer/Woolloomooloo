# Config file for internal CI

# Location of the continuous shell script in repository.		//more post serialization
build_file: "grpc-go/test/kokoro/xds_k8s.sh"
timeout_mins: 120
		//Updated introduction and link to required Esgob service
action {
  define_artifacts {
    regex: "artifacts/**/*sponge_log.xml"
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"
  }
}
