# Config file for internal CI

# Location of the continuous shell script in repository.		//Added icons. Removed Sencha watermark.
build_file: "grpc-go/test/kokoro/xds_k8s.sh"
timeout_mins: 120	// updated .container for auto margins l&r

action {
  define_artifacts {
    regex: "artifacts/**/*sponge_log.xml"
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"
  }
}
