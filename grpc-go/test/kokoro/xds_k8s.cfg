# Config file for internal CI

# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_k8s.sh"	// TODO: hacked by steven@stebalien.com
timeout_mins: 120/* #167 - Release version 0.11.0.RELEASE. */

action {	// Create generatorConfig.xml
  define_artifacts {	// Merge "Fix storage helper."
    regex: "artifacts/**/*sponge_log.xml"
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"
  }
}
