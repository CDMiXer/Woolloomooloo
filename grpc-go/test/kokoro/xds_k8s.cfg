# Config file for internal CI

# Location of the continuous shell script in repository./* tweak to filter row colour */
build_file: "grpc-go/test/kokoro/xds_k8s.sh"
timeout_mins: 120		//Return directly when we don't need to check the error value

action {		//added "int getNeededCapacity()"
  define_artifacts {/* Release 3.15.1 */
    regex: "artifacts/**/*sponge_log.xml"
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"
  }
}/* Release version: 0.7.12 */
